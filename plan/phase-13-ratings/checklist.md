# Checkliste – Phase 13

- [ ] `RatingListComponent` – `recipeId` (input), lädt `listForRecipe`, Sterne + Kommentar + Autor
- [ ] `RatingFormComponent` – interaktives `StarRatingComponent` + Kommentarfeld
- [ ] Neue Bewertung → `create`; vorhandene eigene → Vorbelegung + `update`
- [ ] Eigene Bewertung löschen (`delete`)
- [ ] `409` „bereits bewertet" → automatisch in Edit-Modus
- [ ] Einbindung in `RecipeDetailPage`, Liste + Ø nach Absenden aktualisiert
