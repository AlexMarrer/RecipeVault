# Checkliste – Phase 24

- [ ] `frontend/README.md` mit Start-/Test-Anleitung + Test-User aktualisiert
- [ ] `.gitignore` schließt `node_modules/`, `dist/`, `target/` aus
- [ ] Frische Verifikation: `npm ci && ng build && npm test` grün
- [ ] Alles committet, `git status` ist leer
- [ ] Auf GitHub gepusht
- [ ] Plan-Status final auf ✅ aktualisiert (README im plan-Ordner)
- [ ] ZIP `uscata.alex_m294.zip` erstellt
- [ ] ZIP enthält Frontend **inkl. `.git`**, ohne `node_modules`/`dist`
- [ ] ZIP enthält Backend ohne `target`
- [ ] ZIP enthält die Projektdoku als PDF
- [ ] ZIP-Inhalt einmal stichprobenartig entpackt und geprüft
