# Acceptance Criteria – Phase 24

- `frontend/README.md` erklärt Start (Backend/Keycloak/Frontend) und Tests nachvollziehbar
  und nennt die Test-User je Rolle.
- Repository ist committet und auf GitHub gepusht; `git status` ist sauber.
- `npm ci && ng build && npm test` laufen aus einem frischen Checkout grün.
- Das ZIP heißt `uscata.alex_m294.zip` und enthält:
  - das Frontend **inklusive `.git`-Verzeichnis**, ohne `node_modules`/`dist`,
  - das Backend ohne `target`,
  - die Projektdokumentation als PDF.
- Der Plan-Status ist final aktualisiert.
