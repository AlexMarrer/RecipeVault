# Phase 8 – Verifikation (Build)

**Status:** ⬜ offen · **Typ:** Check · **baut auf:** alle vorigen

## Ziel & Anforderungen

Abschluss-Check, dass beide Projekte sauber kompilieren – das Backend ohne Fehler, das
Frontend als reines SPA-Build (ohne SSR-Artefakte). Damit ist das Grund-Setup nachweislich in
einem konsistenten, baubaren Zustand, bevor die Feature-Phasen (CRUD-Seiten, Formulare, Tests)
beginnen.

## Betroffene Dateien

- keine Änderungen – nur Build-Läufe in `backend/` und `frontend/`
