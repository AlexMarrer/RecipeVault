# Acceptance Criteria – Phase 8

- Backend-Build endet mit `BUILD SUCCESS`.
- Frontend-Build endet ohne Fehler und erzeugt nur ein Browser-Bundle (kein `server/`).
- Keine offenen TS-/Lint-Fehler aus den Phasen 2–7.
