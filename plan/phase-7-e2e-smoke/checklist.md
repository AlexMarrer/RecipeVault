# Checkliste – Phase 7

- [x] `NavbarComponent` (Login/Logout + Username)
- [x] `RecipeListPage` (Titel aus `RecipeService.list()`)
- [x] `app.routes.ts`: `/recipes` (authGuard), `/403`, `**`
- [x] `app.ts`: Navbar + `<router-outlet>`
- [ ] Manueller Lauf: Login → Rezepte sichtbar (braucht Phase 0 + laufende Server)
