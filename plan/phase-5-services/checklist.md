# Checkliste – Phase 5

- [ ] `RecipeService` (CRUD, `list(filter)`, `myRecipes()`)
- [ ] `CategoryService` (CRUD)
- [ ] `IngredientService` (CRUD)
- [ ] `RatingService` (`listForRecipe` → `?recipeId=`, `myRatings()`, CRUD)
- [ ] `AuthService` (`login/logout/isLoggedIn/hasRole/getUserId/roles`)
