# Acceptance Criteria – Phase 4

- Alle sechs Ordner existieren unter `src/app/`.
- Jedes Interface-Feld entspricht 1:1 dem zugehörigen Backend-DTO (Name + Typ).
- IDs korrekt typisiert: Recipe/Category/Ingredient/Rating-ID = `number`, author/user = `string` (UUID).
- `ng build` kompiliert die Modelle ohne TS-Fehler.
